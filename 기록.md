수행: 접수 화면 폼에 "백만수 / 한식조리기능사" 넣고 제출 기대: Supabase applications 테이블에 행 1개 생성 실제: 화면이 "접수 중..."에서 멈추고 콘솔에 [임시 저장 - Supabase 미연결] 로그만 찍힘 조치: common.js의 SUPABASE_URL/SUPABASE_ANON_KEY가 빈 값이었음 → 값 채워 넣고 index.html의 supabase-js 스크립트 주석 해제 재실행: Table Editor에서 행 1개 생성 확인 판정: PASS

수행: 어드민 화면(admin.html)에서 로그인 시도 기대: 로그인 성공 후 대시보드로 전환 실제: "Supabase 연결 정보(common.js)가 비어 있어 로그인할 수 없습니다" 문구 표시 조치: Supabase 대시보드 > Authentication > Users에서 직원 계정 생성 (Auto Confirm User 켬) 재실행: 발급받은 이메일/비밀번호로 로그인 성공, 대시보드 진입 확인 판정: PASS

수행: 접수 화면에서 신청서 재제출 (연결 정보 다 채운 뒤) 기대: DB에 정상 저장 실제: 네트워크 요청이 403으로 실패, 응답에 code: 42501, message: new row violates row-level security policy 표시 조치: 1차로 INSERT RLS 정책을 SQL Editor에서 재생성 → 그래도 동일 오류 재실행: pg_policies / pg_class로 RLS 상태 전체 진단 → 정책·GRANT 모두 정상으로 확인됨 판정: FAIL (원인 미해결 상태로 다음 항목으로 이어짐)

수행: 위 403 오류 원인 추가 조사 — 로그인 세션 유무 확인 기대: 로그아웃 상태에서 접수하면 정상 작동할 것으로 추정 실제: 로그아웃 후 재시도하니 정상 접수됨 → 어드민 로그인 세션이 유지된 채 접수하면 요청이 anon이 아니라 authenticated로 나가는데, INSERT 정책이 anon에게만 열려 있어 로그인 상태에서 오히려 막히는 것으로 확인 조치: INSERT 정책 대상을 anon에서 anon, authenticated로 확장 재실행: 로그인 상태·로그아웃 상태 둘 다 접수 정상 작동 확인 판정: PASS

수행: 챗봇에 "한식조리기능사 응시료가 얼마예요" 등 테스트 질문 9~11개 입력 기대: 자격증별로 정확한 FAQ가 매칭되고, 범위 밖 질문엔 "모르겠습니다"로 답할 것 실제: 조사(가/는/이/을)가 붙은 단어가 원본 키워드와 다른 토큰으로 처리돼 엉뚱한 FAQ와 매칭됨 (예: "굴착기 시험비 얼마예요" → "공인중개사 응시료"로 잘못 매칭) 조치: 어미/조사 정규화(SUFFIXES) + 불용어 제거(STOPWORDS) + 짧은 단어 오매칭 방지 + 키워드 구체화를 반복 적용 재실행: 테스트 질문 11개 전부 정답 매칭 확인 판정: PASS

수행: FAQ 어드민에서 항목 하나 수정 후 챗봇에 같은 질문 재입력 기대: 재배포 없이 수정 내용이 챗봇 답변에 바로 반영될 것 실제: (Supabase faq_items 테이블 이관 + api/chat.py가 파일 대신 테이블을 조회하도록 변경한 뒤 테스트) 조치: 없음 (설계대로 작동) 재실행: FAQ 어드민에서 수정 → 챗봇 재질문 → 수정된 내용으로 답변 확인 판정: PASS

수행: QA 체크리스트 M2 - 접수 화면에서 자격증 카드 클릭 기대: 카드를 클릭하면 신청서 입력 화면으로 전환될 것 실제: 클릭해도 아무 반응 없음 조치: .gitignore에 common.js/script.js가 등록돼 있어 두 파일이 GitHub에 커밋된 적이 없음을 발견 → Supabase anon key는 공개돼도 되는 키라 숨길 필요가 없었음 → .gitignore에서 두 줄 제거 후 git add/commit/push로 재배포 재실행: Vercel 배포는 정상 완료됐으나, 카드 클릭 시 여전히 화면 전환 안 됨 판정: FAIL (배포 문제는 해결됐으나 근본 원인 아니었음, 다음 항목으로 이어짐)

수행: 위 M2 재현 - 배포된 script.js를 직접 열어 원인 재조사 기대: 배포 문제가 해결됐으니 정상 작동할 것으로 예상 실제: goToStep() 함수가 참조하는 stepSelect/stepForm/stepDone과 applyForm/doneSummary 등 여러 변수가 파일 어디에도 선언돼 있지 않음을 발견. 이전에 클릭 핸들러를 방어적으로 리팩터링하는 과정(DOMContentLoaded로 감싸는 수정)에서 최상위 변수 선언부가 통째로 누락된 것으로 확인. 카드 클릭 이벤트 자체는 걸리지만 goToStep() 호출 시 ReferenceError로 중단되어 화면 전환이 안 되는 것이었음 조치: 검증 완료된 원본 script.js(최상위에 모든 DOM 참조가 제대로 선언된 버전)로 전체 교체 재실행: 정상작동

수행: QA 체크리스트 C2 - "100건 표시" 테스트를 위한 더미 데이터 준비 기대: CSV import로 100건을 넣으면 어드민에 100건이 표시될 것 실제: Supabase 대시보드 CSV 업로드는 자격증별 필수 필드(CHECK 제약)와 컬럼 형식을 맞추기 번거로워, 같은 목적을 SQL로 대체하기로 결정 조치: qa_seed_100.sql 작성 - 자격증 3종에 랜덤 분산되도록 100건을 PL/pgSQL로 생성. fee_amount/receipt_number/grade 등 트리거가 자동 계산하는 컬럼은 제외하고 신청자 입력값만 채움 재실행: 정상작동


ai를 사용해서 전체적으로 수정을 진행하다보니 멀쩡하던 부분이 문제가 생기는 경우가 자주 발생함. 